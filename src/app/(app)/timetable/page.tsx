import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@/components/ui/card";

export default function TimetablePage() {
  return (
    <Card>
        <CardHeader>
            <CardTitle className="text-3xl font-bold">Timetable</CardTitle>
            <CardDescription>View timetables for classes, exams, and events.</CardDescription>
        </CardHeader>
        <CardContent>
            <p>This page is under construction.</p>
        </CardContent>
    </Card>
  )
}
